name: Deploy to GitHub Pages

on:
  push:
    branches: ["main"]

permissions:
  contents: write
  # 以下是方案一（官方直接部署）所需的权限，目前已注释
  # pages: write
  # id-token: write

jobs:
  build_site:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: "20"

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build
        run: bun run build

      # --- 注释 ---
      # - name: Upload Artifacts
      #   uses: actions/upload-pages-artifact@v3
      #   with:
      #     path: "dist/"
      # --- 之前的内容结束 ---

      # --- 方案二：推送构建产物到 gh-pages 分支 ---
      - name: Deploy to gh-pages Branch
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist # 确保这是你 bun run build 生成的文件夹
          branch: gh-pages # 目标分支名
      # --- 方案二结束 ---

  # --- deploy job ---
  # deploy:
  #   needs: build_site
  #   runs-on: ubuntu-latest
  #   environment:
  #     name: github-pages
  #     url: ${{ steps.deployment.outputs.page_url }}
  #   steps:
  #     - name: Deploy
  #       id: deployment
  #       uses: actions/deploy-pages@v4
  # --- deploy job 结束 ---